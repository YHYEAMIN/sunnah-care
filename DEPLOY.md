# 🚀 সুন্নাহ কেয়ার সাইট পাবলিক করুন — সবচেয়ে সহজ উপায় (Render.com, ফ্রি)

পুরো ব্যবস্থা (🛒 ল্যান্ডিং পেজ + 📦 অর্ডার সিস্টেম + 🔐 অ্যাডমিন প্যানেল) একসাথে ইন্টারনেটে ছাড়ুন।
সময় লাগবে: ১০-১৫ মিনিট | খরচ: ০ টাকা

---

## 📁 কোন ফাইল কী

| ফাইল | কাজ |
|---|---|
| `index.html` | কাস্টমারদের ল্যান্ডিং পেজ |
| `admin.html` | আপনার অ্যাডমিন প্যানেল |
| `server.py` | সার্ভার — অর্ডার সেভ + অ্যাডমিন API |
| `admin_server.py` | অ্যাডমিন আলাদা চালানোর (Render-এ লাগবে না) |
| `requirements.txt` | Render-এর জন্য (খালি, কিছু ইনস্টল লাগে না) |
| `render.yaml` | Render অটো-কনফিগ ফাইল |
| `data/orders.json` | সব অর্ডার এখানে জমা হয় |
| `data/settings.json` | IP ব্লক ও টেলিগ্রাম সেটিংস |

---

## ✅ ধাপ ১: GitHub-এ ফাইল তোলা (৫ মিনিট)

1. ব্রাউজারে **github.com** খুলুন → **Sign up** → নাম-ইমেইল-পাসওয়ার্ড দিয়ে ফ্রি অ্যাকাউন্ট খুলুন
2. লগইন করে উপরে ডানে **+** → **New repository**
3. Repository name: `sunnah-care` → **Create repository**
4. পরের পেজে **"uploading an existing file"** লিংকে ক্লিক করুন
5. `neuro-guard-site.zip`-এর ভেতরের **সব ফাইল** টেনে এনে ছেড়ে দিন
   (⚠️ `data` ফোল্ডারের ২টা ফাইল যেন ভুলে না যায় — GitHub ফোল্ডার নিজে বানায় না,
   তাই আপলোড বক্সে `data/orders.json` লেখা অংশে ফাইলটি দিলে ফোল্ডার হয়ে যাবে)
6. নিচে **Commit changes** → **Commit changes**

## ✅ ধাপ ২: Render-এ ফ্রি হোস্ট (৫ মিনিট)

1. ব্রাউজারে **render.com** খুলুন → **Get Started** → **GitHub দিয়ে সাইন আপ** (অনুমতি দিন)
2. ড্যাশবোর্ডে **New +** → **Web Service**
3. লিস্টে আপনার **sunnah-care** রিপো দেখবেন → **Connect**
4. যেহেতু `render.yaml` আছে, Render অটো সব বুঝে নেবে। শুধু চাইলে:
   - **Name:** `sunnah-care` (এটাই লিংকের নাম হবে)
   - **Instance Type:** **Free** নিশ্চিত করুন
   - **ADMIN_PASSWORD** জিজ্ঞেস করলে **নিজের নতুন পাসওয়ার্ড** দিন (ডিফল্ট `sunnah2026` না!)
5. **Create Web Service / Apply** → ২-৩ মিনিট বিল্ড হবে (Log চলতে দেখবেন)

## ✅ ধাপ ৩: লাইভ! 🎉

বিল্ড শেষ হলে উপরে লিংক পাবেন:

| কী | লিংক |
|---|---|
| 🛒 কাস্টমার পেজ | `https://sunnah-care.onrender.com/` |
| 🔐 আপনার অ্যাডমিন | `https://sunnah-care.onrender.com/admin` |

- ফেসবুকে কাস্টমার লিংক শেয়ার করুন
- অ্যাডমিন লিংকটা **কাউকে দেবেন না!**
- অ্যাডমিনে ঢুকে 📤 টেলিগ্রাম সেট করে নিন — প্রতিদিনের রিপোর্ট ফোনে পাবেন

---

## ⚠️ ফ্রি প্ল্যানের ৩টা সীমা (জরুরি)

1. **ঘুম:** ১৫ মিনিট কেউ না ঢুকলে সার্ভার ঘুমায় — পরের ভিজিটে ৩০-৬০ সেকেন্ড লাগে (রিফ্রেশ করুন)
2. **ডেটা রিসেট:** কয়েক দিন পরপর সার্ভার রিস্টার্ট হলে **জমা অর্ডার মুছে যেতে পারে!**
   → প্রতিদিন অ্যাডমিন থেকে **📄 CSV ডাউনলোড** করে রাখুন ✅
   → অর্ডার এলেই অ্যাডমিনে দেখে নোট/ফোন কনফার্ম করুন
3. **টেলিগ্রাম রিপোর্ট** দিনশেষে নিজে পাঠিয়ে নিন (অ্যাডমিনের 📤 বাটন)

💼 ব্যবসা বড় হলে Render **Starter $7/মাস** নিন — ঘুমাবে না, ডেটাও বেশি স্থায়ী হবে।

---

## 🔧 সমস্যা হলে

| সমস্যা | সমাধান |
|---|---|
| বিল্ড Fail | Start Command ঠিক আছে কি দেখুন: `python server.py` |
| সাইট খুলছে না | ১ মিনিট অপেক্ষা করে রিফ্রেশ (ঘুম থেকে উঠছে) |
| পাসওয়ার্ড ভুলে গেছেন | Render → আপনার Service → Environment → ADMIN_PASSWORD বদলান |
| নতুন কোড দিতে চান | GitHub-এ ফাইল এডিট/আপলোড → Render নিজেই আবার ডিপ্লয় করবে |
